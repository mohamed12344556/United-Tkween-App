import 'package:flutter/material.dart';

class AppShadows {
  AppShadows._();
  static const BoxShadow shadow1 = BoxShadow(
    color: Colors.black26,
    blurRadius: 10,
    offset: Offset(0, 5),
  );
}
